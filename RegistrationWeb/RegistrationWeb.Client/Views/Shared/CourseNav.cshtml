@model RegistrationWeb.Client.Models.CourseListViewModel

<ul class="nav nav-tabs">
    @Html.LinkItem(Model.CurrentAction, "Show", "/course/" + Model.CurrentCourseId, "Scheduled Courses")
    @Html.LinkItem(Model.CurrentAction, "New", "/course/" + Model.CurrentCourseId + "/new", "Add Course Schedule")
</ul>
