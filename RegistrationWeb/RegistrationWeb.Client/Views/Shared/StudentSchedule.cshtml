@model RegistrationWeb.Domain.RegistrationServiceReference.StudentScheduleDAO

<table class="table table-hover">
    <colgroup>
        <col style="width: 50%" />
        <col style="width: 50%" />
    </colgroup>

    <thead>
        <tr>
            <td><h3>@Model.CourseSchedule.Course.Title</h3></td>
            <td style="vertical-align: bottom">
                @using (Html.BeginRouteForm("DropStudentCourse", new { studentId = Model.Student.Person.Id, courseScheduleId = Model.CourseSchedule.Id }))
                {
                    @Html.Hidden("redirectSuccess", "Show")
                    @Html.Hidden("redirectFailure", "Show")
                    <button type="submit" data-toggle="tooltip" title="Drop Course" class="btn btn-danger pull-right">
                        <span class="glyphicon glyphicon-remove"></span>
                    </button>
                }
            </td>
        </tr>
    </thead>

    <tbody>
        <tr>
            <th>Professor</th>
            <td>@Model.CourseSchedule.Professor.Name</td>
        </tr>

        <tr>
            <th>Start Time</th>
            <td>@Html.TimeFormat(Model.CourseSchedule.Schedule.StartTime)</td>
        </tr>

        <tr>
            <th>End Time</th>
            <td>@Html.TimeFormat(Model.CourseSchedule.Schedule.EndTime)</td>
        </tr>

        <tr>
            <th>Students Enrolled</th>
            <td>@Model.CourseSchedule.Enrolled / @Model.CourseSchedule.Capacity</td>
        </tr>
    </tbody>
</table>

